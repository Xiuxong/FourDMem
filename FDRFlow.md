Agent Query
     │
     ▼
┌─────────────┐
│ Layer Router │ ← 意图识别：事实/原因/规范/历史
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│ Temporal Gate    │ ← 默认剔除过期版本；at_time 对齐
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Multi-Path Recall │ ← 向量 + 全文 + 图遍历 (按路由层级)
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ RRF Fusion       │ ← 三路融合
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Layer-Aware RIF  │ ← 同层内按 RIF 排序
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Quota Allocator  │ ← L3(300t)+L2(600t)+L1(450t)+L0(150t)
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ On-Demand Summary│ ← 长文本自动摘要 + original_ref
└──────┬───────────┘
       │
       ▼
   Response ≤ 1500 Tokens
   (含 drill_down hint)
